pipeline {
    agent any
    stages {
        stage('build') {
            steps {
 //              echo "buildapp"
               version "python --version"
            }
        }
 //   stage('test') {
 //         steps {
 //            echo "testapp"
 //         }
 //     }
    }
}
